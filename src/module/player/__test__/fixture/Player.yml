entity: Player
items:
  player1:
    name: 'player'
    metadataUri: 'http://example.com.ar'
    externalId: '{{string.uuid}}'
    issuer: 'ISSUER'
    owner: '@admin-user'
  player2:
    name: '{{string.alpha}}'
    metadataUri: '{{string.alpha}}'
    externalId: '{{string.uuid}}'
    issuer: '{{string.alpha}}'
    owner: '@admin-user'